# Task: Docs Rewrite Part F — User Documentation Rewrite

## Problem Statement

The 4 surviving `docs/user/` files (854 + 938 + 804 + 311 = ~2,900 lines) contain pervasive stale content: TensorFlow references (ONNX is the ML backend), queue-based processing language (discovery workers replaced queues), nonexistent CLI commands (`admin-reset`, `cache-refresh`, `remove` — only `cleanup` and `manage-password` exist), dead links to the deleted `api_reference.md`, wrong port numbers (8888 vs 8356), and an entirely fictitious Navidrome integration model (TOML file export workflow vs the actual direct-push API with smart playlists, static playlists, and song sync). No user-facing troubleshooting guide exists despite common deployment pitfalls with Docker networking, GPU setup, and model downloads.

## Phases

### Phase 1: Audit and Stale Content Inventory

- [x] Read all 4 user docs against the current codebase and produce a stale content catalog as a scratch note — must identify every instance of: TensorFlow references (→ ONNX Runtime), queue/job language (→ discovery-based workers), dead CLI commands (`admin-reset`, `cache-refresh`, `remove` — actual CLI has only `cleanup` and `manage-password`), `api_reference.md` links (deleted in Plan A → replace with `/docs` FastAPI interactive docs), wrong port 8888 (→ 8356), stale config keys (`processing.workers`, `processing.batch_size`, `navidrome.export_dir`, `navidrome.auto_export`), dead cross-refs to `../dev/services.md` (deleted Plan E) and `../dev/calibration.md`
    **Catalog:** **getting_started.md (854 lines):**
TENSORFLOW → ONNX: L32 "Nomarr uses TensorFlow models for audio analysis", L210 "pip install tensorflow[and-cuda]==2.15.0", L624 "import tensorflow as tf; print(tf.config.list_physical_devices('GPU'))", L186-195 CUDA 11.8 install instructions (ONNX uses different CUDA).
QUEUE/JOB LANGUAGE: L279 "Creates processing queue entries", L311 "Workers continuously pick up jobs from the queue", L325 "Queue depth (pending, running, completed, errors)", L326-332 "Queue page shows" / "Individual job status" / "Error messages for failed jobs" / "Estimated time remaining" / "Worker health (heartbeat status)" / "Current job per worker", L354 "queue them for processing", L516-518 "Queue page" / "Filter by status to see failed jobs".
DEAD CLI: L522-526 nom admin-reset --stuck/--errors (DEAD), L531-532 nom cleanup --hours 168 (cleanup actually removes orphaned entities not old jobs), L547-553 CLI table lists admin-reset, cache-refresh, remove (ALL DEAD, only cleanup and manage-password exist).
API_REFERENCE LINKS: L812 [api_reference.md](api_reference.md) DEAD.
STALE CONFIG: L127 processing.workers, L128 processing.batch_size, L229 navidrome.export_dir in native config, L710-713 processing.batch_size in troubleshooting, L719-721 processing.workers in troubleshooting, L776-787 processing.workers/batch_size in performance tips, L842-843 mentions "processing: Workers, batch size, queue limits".
DEAD CROSS-REFS: L812 api_reference.md (DEAD), L823 ../dev/calibration.md (DEAD, actual file is calibration-troubleshooting.md), L849 ../dev/services.md (DEAD).
OTHER: L222 config/config.yaml should be config/nomarr.yaml, L207 ./scripts/download_models.sh likely nonexistent, L725 ml.gpu_memory_growth stale config, entire native install section (L167-240) references TF+CUDA.
**deployment.md (938 lines):**
QUEUE/JOB: L472-473 processing.workers/gpu_per_worker config, L714-715 db.queue.compact() references nonexistent queue collection, L746-766 worker tuning with processing.workers config.
STALE CONFIG: L472 processing.workers: 4, L473 gpu_per_worker: 0.5, L753 workers: 2.
DEAD CROSS-REFS: L916 api_reference.md DEAD. L917 ../dev/workers.md and L918 ../dev/architecture.md both EXIST and valid.
OVER-ENGINEERED: L484-510 Prometheus/Grafana/DCGM monitoring and L443 /metrics on port 9090 excessive for alpha.
**navidrome.md (804 lines):**
TOML EXPORT MODEL (ENTIRE FILE): L15-18 TOML format and regular exports, L22-39 "How It Works" TOML export pipeline diagram, L42-57 example TOML format, L78-129 config with export_dir/auto_export/export_interval/YAML rules, L131-159 Navidrome Playlist Directory with Docker volume mount/rsync, L175-182 Automatic Export section, L369 using playlists with navidrome --scan, L580-594 troubleshooting auto_export/export_interval, L607-659 example workflow with manual scan, L676-699 advanced YAML configs. Essentially entire file describes fictional TOML export workflow.
PORT 8888: L730, L738, L746 all use localhost:8888.
STALE CONFIG: L82 export_dir, L83 auto_export, L84 export_interval, L589/L594 same, L95-120 YAML playlist rules.
DEAD CROSS-REFS: L753/L789 api_reference.md DEAD, L789 ../dev/calibration.md DEAD.
**playlist_import.md (311 lines):**
DEAD CROSS-REFS: L157/L307 api_reference.md#playlist-import-endpoints DEAD.
STALE CONFIG: L91 config.yaml should be nomarr.yaml.
NOTE: File is largely accurate otherwise. Main fixes are api_reference.md links → /docs and config.yaml → nomarr.yaml.
- [x] Verify the actual Navidrome integration model by reading `navidrome_if.py` endpoints and frontend `features/navidrome/` — document what the current UI and API actually support (smart playlists via .nsp generation, static playlists, push-to-Navidrome via Subsonic API, song sync, ping/status, templates) vs. what `navidrome.md` describes (TOML file export + copy to Navidrome directory + Navidrome rescan)
    **Findings:** ACTUAL NAVIDROME INTEGRATION (from navidrome_if.py + frontend):
Backend API (12 endpoints): GET /preview (tag stats), GET /tag-values (distinct values per tag rel), GET /config (generate TOML config text), POST /playlists/preview (query preview), POST /playlists/generate (generate .nsp file), GET /templates (list templates), POST /templates (batch generate), POST /playlists/static (M3U from file IDs), POST /playlists/push (push to Navidrome via Subsonic API), POST /sync-songs (sync Navidrome songs to graph), POST /ping (test connectivity), GET /status (check if configured).
Frontend UI (NavidromePage.tsx, 3 accordions): (1) Generate Config — preview tag stats, generate TOML config text. (2) Playlist Maker — visual RuleBuilder with nested AND/OR groups, tag/operator/value rules, SortPicker, limit, preview matching tracks, generate .nsp file download. (3) API Settings — configure Navidrome connection (navidrome_api_url, navidrome_api_user, navidrome_api_password via config API), ping test button, song sync trigger, API key management with copy/regenerate.
KEY DIFFERENCES (docs vs reality):
(1) Connection model: Docs describe file-system coupling (volume mounts, rsync, export_dir). Reality: Navidrome server connection via Subsonic API with URL/user/password configured in API Settings panel.
(2) Playlist delivery: Docs describe TOML file export → copy to Navidrome dir → Navidrome rescan. Reality: .nsp files generated client-side for download PLUS direct push to Navidrome via Subsonic API (POST /playlists/push resolves Nomarr file IDs to Navidrome song IDs).
(3) Song sync: Docs don't mention it. Reality: POST /sync-songs pulls Navidrome's library into Nomarr's graph collections for cross-referencing.
(4) Static playlists: Docs don't mention M3U generation from file IDs. Reality: POST /playlists/static generates M3U.
(5) Ping/status: Docs don't mention connectivity testing. Reality: POST /ping tests Navidrome reachability, GET /status checks if configured.
(6) Templates: Docs mention templates vaguely in Web UI section. Reality: dedicated GET/POST /templates endpoints with batch generation.
(7) Config keys: Docs reference export_dir, auto_export, export_interval in nomarr.yaml. Reality: navidrome_api_url, navidrome_api_user, navidrome_api_password stored via config API.
(8) Playlist format: Docs describe TOML with [[rules]] blocks. Reality: .nsp files with query syntax like "tag:electronic > 0.7 AND tag:danceable > 0.7", nested groups supported up to depth 5.
CONCLUSION: navidrome.md needs a near-complete rewrite. The TOML export + file copy + Navidrome rescan model is entirely fictional. The actual model is: configure Navidrome connection → sync songs → build smart playlists via rule builder → preview → generate .nsp or push directly via Subsonic API.

### Phase 2: Rewrite All User Documentation

- [x] Rewrite `getting_started.md` — replace TensorFlow with ONNX Runtime throughout; remove native installation section (Docker-only for user docs); replace all queue/job language with discovery worker model; fix CLI reference table to only `cleanup` and `manage-password`; replace `api_reference.md` links with `/docs` endpoint; remove stale config keys; fix cross-references to files surviving Plan E; keep tone approachable and step-by-step for non-technical users
    **Notes:** Rewrote from 854 lines to ~545 lines. Removed native installation section entirely (Docker-only). Replaced all TensorFlow references with ONNX Runtime. Replaced queue/job language with discovery worker model. Fixed CLI table to only cleanup and manage-password with accurate subcommands. Replaced api_reference.md links with /docs endpoint and troubleshooting.md. Removed stale config keys (processing.workers, batch_size, navidrome.export_dir). Fixed cross-references: calibration-troubleshooting.md, removed ../dev/services.md. Removed Performance Tips section with stale config. Updated compose.yaml to match actual (arangodb:latest, --vector-index command, stop_grace_period).
- [x] Rewrite `deployment.md` — remove stale `queue.compact()` ArangoDB reference; replace processing worker/batch_size config with current config model; fix `api_reference.md` and `../dev/workers.md`/`../dev/architecture.md` cross-references; scale back over-engineered production ops sections (Prometheus/Grafana/DCGM monitoring) to match alpha software status; verify compose.yaml snippets against actual `docker/compose.yaml`; keep security hardening and backup sections but verify accuracy
    **Notes:** Rewrote from 938 lines to ~646 lines. Removed queue.compact() ArangoDB reference. Replaced processing.workers/batch_size/gpu_per_worker with tagger_worker_count. Removed Prometheus/Grafana/DCGM monitoring sections (excessive for alpha). Removed /metrics endpoint reference. Fixed compose.yaml to match actual (arangodb:latest, --vector-index, stop_grace_period). Replaced api_reference.md links with /docs endpoint. Kept ../dev/workers.md and ../dev/architecture.md cross-refs (verified they exist). Added troubleshooting.md cross-ref. Simplified update procedure to use docker compose pull (pre-built images). Kept backup, security, and reverse proxy sections with accuracy fixes.
- [x] Rewrite `navidrome.md` — replace the entire TOML file export model with the actual integration: Navidrome server connection via Subsonic API (ping + config), smart playlist generation (.nsp files with query builder), static playlist creation, push-to-Navidrome, song sync, templates; remove all `export_dir`/`auto_export`/TOML template config; fix port 8888 → 8356 in API examples; describe the Web UI workflow accurately based on `features/navidrome/` pages
    **Notes:** Complete rewrite from 804 lines to ~327 lines. Replaced entire fictional TOML export pipeline with actual integration: Subsonic API connection, rule builder with nested AND/OR groups, .nsp file generation, direct push to Navidrome, song sync, static M3U playlists, templates with batch generation, ping/status endpoints. Removed all export_dir/auto_export/export_interval/TOML template config. Fixed port 8888 to 8356 in all API examples. Added complete API endpoint reference table matching actual navidrome_if.py routes. Described Web UI workflow accurately (API Settings panel, Playlist Maker, Generate Config). Removed Docker volume mount / rsync / file copy instructions since delivery is via Subsonic API.
- [x] Freshen `playlist_import.md` — remove `api_reference.md` links (replace with `/docs` endpoint); verify Spotify credential setup and Deezer public API sections against current `playlist_import_if.py`; verify matching tiers and flow against current implementation; ensure config reference uses `nomarr.yaml` not `config.yaml`
    **Notes:** Targeted fixes (4 replacements): config.yaml to config/nomarr.yaml (2 occurrences), api_reference.md links to /docs endpoint (2 occurrences). Verified Spotify credential setup and Deezer public API sections match current playlist_import_if.py (convert_playlist and spotify_credentials_status endpoints). Verified matching tiers (isrc, exact, fuzzy_high, fuzzy_low, none) and flow are accurate. File otherwise correct at 311 lines.
- [x] Create `docs/user/troubleshooting.md` — cover common user issues: Docker networking (containers can't reach each other, ArangoDB health check failures), GPU not detected in containers (NVIDIA Container Toolkit, compose.yaml `deploy.resources`), first-run database provisioning failures, model download/loading issues, scan not finding files (volume mount mismatches, file permissions), Navidrome connection failures (wrong URL, Subsonic API credentials), calibration confusion (minimum track count, when to recalibrate); write in user-friendly language with symptoms → causes → solutions format
    **Notes:** Created new file at docs/user/troubleshooting.md (~403 lines). Covers all 7 required areas: Docker networking (container communication, ArangoDB health check), GPU not detected (NVIDIA Container Toolkit, compose.yaml config, ONNX Runtime providers), first-run database provisioning (password mismatch, arango_password missing), model loading issues, scan not finding files (volume mount mismatch, file permissions, file watcher on network mounts), Navidrome connection failures (ping failures, push failures, song sync), calibration confusion (minimum track count, when to recalibrate). All sections follow symptom/cause/solution format. Includes General Tips section with log checking, restart procedures, and version checking.
- [x] Update `mkdocs.yml` nav to add `Troubleshooting: user/troubleshooting.md` under the User Guide section after Playlist Import
    **Notes:** Added "Troubleshooting: user/troubleshooting.md" to mkdocs.yml nav under User Guide section, after Playlist Import. All 5 user guide pages now listed: Getting Started, Deployment, Navidrome Integration, Playlist Import, Troubleshooting.

### Phase 3: Validation

- [x] Grep all `docs/user/` files for every stale term: `TensorFlow`, `tensorflow`, `queue` (in processing context), `batch_size`, `admin-reset`, `cache-refresh`, `nom remove`, `api_reference.md`, `config/config.yaml`, `export_dir`, `auto_export`, `8888`, `../dev/services.md`, `TOML` (in Navidrome context) — zero matches required except where `queue` appears in non-processing contexts
    **Notes:** All 14 stale terms verified with zero prohibited matches. Searched: TensorFlow, tensorflow, batch_size, admin-reset, cache-refresh, nom remove, api_reference.md, config/config.yaml, export_dir, auto_export, 8888, ../dev/services.md, queue (all contexts). TOML in navidrome.md confirmed NOT stale — endpoint actually produces TOML config (verified via web_navidrome_config source: NavidromeConfigResponse.from_toml()).
- [x] Verify all internal cross-references resolve: every `[text](target.md)` link in `docs/user/` must point to a file that exists; every `../dev/` reference must point to a file surviving Plan E; confirm `mkdocs.yml` nav lists all 5 user guide pages and each file exists at the listed path
    **Notes:** All cross-references validated. Internal user→user links: all resolve (getting_started.md, deployment.md, navidrome.md, playlist_import.md, troubleshooting.md). Dev references: ../dev/workers.md ✓, ../dev/architecture.md ✓, ../dev/calibration-troubleshooting.md ✓ (all confirmed in docs/dev/). mkdocs.yml nav lists all 5 user guide pages at correct paths. No broken links found — zero fixes needed.

## Completion Criteria

- All 4 existing user docs rewritten with zero stale terms from the contracts ledger
- No references to TensorFlow, queue-based processing, deleted `api_reference.md`, or nonexistent CLI commands
- `navidrome.md` describes the actual Subsonic API-based integration, not a TOML file export workflow
- `troubleshooting.md` exists with symptom-based troubleshooting for the 6+ most common user issues
- `mkdocs.yml` nav updated with all 5 user guide pages
- All internal cross-references between user docs and to dev docs resolve to existing files
- Tone throughout is approachable and non-technical — no layer architecture, no code references, step-by-step where appropriate
