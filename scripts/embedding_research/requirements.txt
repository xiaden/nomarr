# Research package dependencies — install inside the devcontainer before running.
#
#   pip install -r /workspace/scripts/embedding_research/requirements.txt
#
# Core ML / metrics (FAISS/ANN dependency removed under Plan D P1-S6 — similarity.py is exact-CPU-only)
scikit-learn>=1.4.0       # MAP, NDCG, pairwise metrics, t-SNE
scipy>=1.12.0
umap-learn>=0.5.6         # UMAP projection for visualisation

# Database / IO
duckdb>=1.5,<2.0           # embedded analytical DB — stores embeddings, metrics, heads; 2.x is a separately approved follow-up
pandas>=2.2.0
tabulate>=0.9.0            # markdown tables from DataFrames
pyarrow>=15.0.0            # fast parquet IO for large result tables

# Plotting (optional — skip if headless)
plotly>=5.20.0


# Core ML / ONNX (must match the main nomarr pyproject.toml dependencies)
onnxruntime-gpu>=1.18.0

# Audio feature extraction (optional — required for patch_features extraction)
librosa>=0.10.0            # audio analysis: RMS, spectral centroid, beat tracking
soundfile>=0.12.0          # fast libsndfile backend for librosa (avoids audioread fallback)

# Utilities
alive-progress>=3.1.0     # pretty progress bars
