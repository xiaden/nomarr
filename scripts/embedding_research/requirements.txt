# Research package dependencies — install inside the devcontainer before running.
#
#   pip install -r /workspace/scripts/embedding_research/requirements.txt
#
# Core ML / ANN
faiss-cpu>=1.8.0          # HNSW / IVF approximate nearest-neighbour
scikit-learn>=1.4.0       # MAP, NDCG, pairwise metrics, t-SNE
umap-learn>=0.5.6         # UMAP projection for visualisation

# Database / IO
duckdb>=0.10.0             # embedded analytical DB — stores embeddings, metrics, heads
pandas>=2.2.0
tabulate>=0.9.0            # markdown tables from DataFrames
pyarrow>=15.0.0            # fast parquet IO for large result tables

# Plotting (optional — skip if headless)
matplotlib>=3.8.0
seaborn>=0.13.0

# Utilities
tqdm>=4.66.0               # progress bars
