# CLAUDE.md

<!-- Add your custom instructions below. Repowise will never modify anything outside the REPOWISE markers. -->
<!-- Examples: coding style rules, test commands, workflow preferences, constraints -->

<!-- REPOWISE:START — Do not edit below this line. Auto-generated by Repowise. -->
## IMPORTANT: Codebase Intelligence Instructions for nomarr

> This repository is indexed by [Repowise](https://repowise.dev).
> Use the MCP tools below for orientation, discovery, and enriched context
> (documentation, ownership, history, decisions). **Always verify against
> actual source files before making changes** — the index may be stale.

Last indexed: 2026-05-28 (commit db2650f6). Confidence: 100%.
### Architecture
repo is a multi-modal media management and analytics engine that ingests raw filesystem data, processes it through an ArangoDB-backed persistence layer, applies ML-driven discovery via ONNX models, and exposes the resulting library insights through a React-based web interface and a Navidrome-compatible plugin. The system orchestrates complex background worker tasks to synchronize library states, curate metadata tags, and perform vector-based search, providing a unified dashboard for large-scale media library administration. *   **Backend:** Python (primary logic, ML discovery, migration scripts), Go (Navidrome plugin integration). *   **Frontend:** TypeScript, React (feature-based architecture, UI component library).
### Key Modules
| Module | Purpose | Owner |
|--------|---------|-------|
| `community-1` | The nomarr/components module serves as the core domain logic and orchestration l | — |
| `community-0` | The frontend/features module serves as the presentation and orchestration subsys | — |
| `community-2` | The nomarr/interfaces module serves as the primary transport and presentation la | — |
| `community-5` | The nomarr/public_html module serves as the client-side presentation layer and r | — |
| `community-3` | The nomarr/components module serves as the core orchestration and processing lay | — |
| `community-4` | The scripts/embedding_research module serves as the experimental evaluation and  | — |
| `community-7` | The nomarr/navidrome module serves as the integration and analytics bridge for t | — |
| `community-6` | The nomarr/persistence module serves as the primary persistence layer for the No | — |
| `community-8` | The scripts/consolidate_migrations module serves as the schema evolution and sta | — |
| `community-9` | The scripts/human-scripts module serves as the static analysis and graph constru | — |
### Entry Points
- `frontend/src/shared/components/ui/index.ts`
- `nomarr/app.py`
- `scripts/diagnostics/agent_dashboard/__main__.py`
- `navidrome-plugin/src/main.go`
- `frontend/src/shared/api/index.ts`
- `frontend/src/App.tsx`
- `docs/index.md`
- `frontend/src/features/admin/index.ts`
- `frontend/src/features/analytics/components/TagCoOccurrenceGrid/index.ts`
- `frontend/src/features/analytics/index.ts`
### Tech Stack
**Languages:** Python
**Frameworks:** FastAPI, HTTPX, TensorFlow

**Infra:** Docker### Architectural Layers
| Layer | Files | Purpose |
|-------|-------|---------|
| Frontend User Interface | 172 | A React-based web interface providing administrative tools, analytics visualizat |
| Core Library Management | 171 | Backend logic for managing library records, file states, and database persistenc |
| API Service Layer | 117 | FastAPI-based interface layer handling authentication, dependency injection, and |
| Machine Learning Integration | 75 | Components for metadata extraction and ONNX-based model inference for audio and  |
| Embedding Research Utilities | 67 | Experimental scripts and research tools for vector embedding strategies, configu |
| Frontend Production Assets | 46 | Contains the compiled JavaScript bundles and UI component assets for the web int |
| Database Persistence Layer | 52 | Handles ArangoDB connectivity, AQL query generation, and data persistence operat |
| Navidrome Integration Service | 45 | Manages data transfer objects and service components for interacting with Navidr |
| Migration Management Tools | 17 | Provides utilities for schema modeling, migration replay, and automated analysis |
| Codebase Analysis Engine | 14 | Implements AST-based parsing and extraction tools to generate structural graphs  |

### Guided Tour (6 steps)
1. **Project Overview and Documentation** — `docs/index.md`
2. **Frontend Entry Point and UI** — `frontend/src/main.tsx`
3. **API Service Layer** — `nomarr/app.py`
4. **Core Library and Persistence** — `nomarr/persistence/db.py`
5. **Machine Learning and Data Models** — `nomarr/helpers/dataclasses.py`
6. **Testing Infrastructure** — `tests/conftest.py`
### Hotspots (High Churn)
| File | Churn | 90d Commits | Owner |
|------|-------|-------------|-------|
| `nomarr/persistence/db.py` | 99.9th %ile | 18 | Lucian Hardy |
| `tests/unit/components/library/test_library_file_query_comp.py` | 99.8th %ile | 20 | Lucian Hardy |
| `nomarr/persistence/database/file_states_aql.py` | 99.6th %ile | 13 | Lucian Hardy |
| `tests/unit/persistence/database/test_file_states_aql.py` | 99.5th %ile | 10 | Lucian Hardy |
| `nomarr/components/library/scan_lifecycle_comp.py` | 99.4th %ile | 28 | Lucian Hardy |

## Code health
Hotspot health: 6.91/10 (stable) ·
Average: 7.29/10 ·
Worst: 1.0/10 (`nomarr/services/infrastructure/workers/discovery_worker.py`)

### Critical biomarkers
- `nomarr/components/tagging/tagging_aggregation_comp.py` — untested hotspot — impact −2.0
- `nomarr/helpers/dto/navidrome_dto.py` — untested hotspot — impact −2.0
- `nomarr/interfaces/api/web/library_if.py` — untested hotspot — impact −2.0
- `nomarr/interfaces/api/web/calibration_if.py` — untested hotspot — impact −2.0
- `nomarr/services/domain/vector_search_svc.py` — untested hotspot — impact −2.0

### Repowise MCP Tools

This repo has the Repowise MCP server configured. The tools below answer questions `grep`/`Read` cannot. Every response carries an `_meta` envelope with `index_age_days`, `indexed_commit`, and a `stale_warning` only when the index has actually diverged from HEAD — silence means the index is current.

**When to call which tool:**

| Tool | What only this tool answers |
|------|------------------------------|
| `get_answer(question)` | Synthesised answer with verified citations and a calibrated `retrieval_quality`. First call for "how does X work" / "why is Y like this". On low confidence returns `best_guesses` with one-line justifications instead of an empty answer. |
| `get_context(targets=[...])` | Triage card for files/modules/symbols — title, summary, signatures, `hotspot` bit, `decision_records` titles, and `symbol_id`s to pipe into `get_symbol`. Use `include=["callers","ownership",...]` to widen. NOT for source bytes. |
| `get_symbol("path/to/file.py::Name")` | Raw source bytes for one indexed symbol with exact line bounds. Cheaper and safer than `Read` + offset math. Use the `symbol_id` returned by `get_context`. |
| `search_codebase(query, kind?)` | Find pages by concept when you don't know the file. Each result carries `search_method` (`embedding` vs `bm25` fallback). For exact identifiers use Grep — the tool will hint when it sees one. |
| `get_why(query, targets?)` | Architectural decision archaeology — *why* the code is shaped this way. Call before refactors or pattern divergences. Falls back to git archaeology when no ADRs exist for a file. |
| `get_risk(targets, changed_files?)` | What history says about touching these files: churn, owners, blast radius. Pass `changed_files` for PR mode → returns a `directive` (`will_break`, `missing_cochanges`, `missing_tests`). |
| `get_dead_code(...)` | Tiered unreachable / unused-export / zombie-package findings. Run before a cleanup sprint, not before a targeted fix. |
| `get_overview(repo?)` | Architecture map for an unfamiliar repo. One-time orientation; skip on subsequent calls in the same session. |

**Composition tips:**
- `get_answer` → if `confidence` is `medium`/`low`, follow the `best_guesses[0].file` or `fallback_targets[0]` into `get_context`, then `get_symbol` for bytes.
- `get_context` returns `decision_records` titles → call `get_why(targets=[...])` for the rationale.
- `get_context` returns `hotspot: true` → call `get_risk` before editing.
- PR review → `get_risk(targets=[...], changed_files=[...])`; read the `directive` block first.

**Verify when:** `_meta.stale_warning` is present, or `retrieval_quality` is `partial`/`weak`, or `search_method` is `bm25`. Otherwise trust the response and act on it.

### Codebase Conventions
**Commands:**
- Lint: `npm run lint`
- Test: `pytest`
- Format: `ruff format .`
- Typecheck: `mypy .`

<!-- REPOWISE:END -->
