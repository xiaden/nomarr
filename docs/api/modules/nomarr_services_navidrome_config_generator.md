# nomarr.services.navidrome.config_generator

API reference for `nomarr.services.navidrome.config_generator`.

---

## Functions

### generate_navidrome_config(db: 'Database', namespace: 'str' = 'nom') -> 'str'

Generate Navidrome TOML configuration for custom tags.

### preview_tag_stats(db: 'Database', namespace: 'str' = 'nom') -> 'dict[str, dict[str, Any]]'

Preview statistics for all tags in the library.

---
