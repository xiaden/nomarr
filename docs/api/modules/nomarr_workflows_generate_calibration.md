# nomarr.workflows.generate_calibration

API reference for `nomarr.workflows.generate_calibration`.

---

## Functions

### generate_calibration_workflow(db: 'Database', models_dir: 'str', namespace: 'str', thresholds: 'dict[str, float] | None' = None) -> 'dict[str, Any]'

Generate calibrations for all heads and track drift metrics.

---

## Constants

### TYPE_CHECKING

```python
TYPE_CHECKING = False
```

---
