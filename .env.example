# Environment Variables for Nomarr
# Copy this file to .env and customize values

# ArangoDB Root Password (FIRST-RUN ONLY)
# Used ONLY during initial onboarding to create database and app user
# App generates its own password and stores in config after first boot
# Change this to a strong password in production
ARANGO_ROOT_PASSWORD=change_this_root_password_in_production
