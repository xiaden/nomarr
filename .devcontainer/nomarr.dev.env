# Nomarr dev environment variables.
# Used by docker-compose.dev.yaml — NOT for production.

# ArangoDB connection (service name on the internal Docker Compose network)
ARANGO_HOST=http://nomarr-arangodb-dev:8529

# Root password — must match nomarr-arangodb.dev.env
ARANGO_ROOT_PASSWORD=nomarr_dev_password
