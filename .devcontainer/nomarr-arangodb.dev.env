# ArangoDB dev environment variables.
# Used by docker-compose.dev.yaml — NOT for production.

# Root password — used only during initial provisioning.
# Must match ARANGO_ROOT_PASSWORD in nomarr.dev.env.
ARANGO_ROOT_PASSWORD=nomarr_dev_password

# Disable anonymous access (always keep 0)
ARANGO_NO_AUTH=0
