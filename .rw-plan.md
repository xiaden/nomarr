# Goal
Perform code maintenance passes over the backend components, moving to have a better composition, DRY patterns, typing closer to the examples in V2, and clear, meaningful logging/error tracing @nomarr/components/. Additionally, remove all AI generated slop introduced in backend code files.

This includes:
- Extra comments that a human wouldn't add or is inconsistent with the rest of the file
- Extra defensive checks or try/catch blocks that are abnormal for that area of the codebase
- Casts to any to get around type issues
- Any other style that is inconsistent with the file
- Unnecessary emoji usage

# Baseline
- **Tests:** 1715 passed, 2 skipped, 0 failed (pre-existing). No regressions.
- **Lint (ruff):** 0 errors, 0 warnings on components (pre-existing).
- **Type check (mypy):** 0 errors on components (pre-existing).

# Sub-tasks

Each sub-task eliminates verbose Google-style docstrings (Args/Returns/Raises/Examples blocks) and AI slop (em-dash prefixes, section separators, redundant inline restatements) from its scoped files. The target convention is single-line docstrings matching the style in tag_query_comp.py and library_records_comp.py.

## S1: Library — tag_hydration_comp.py
- **Depends on:** none
- **Scope:** `nomarr/components/library/tag_hydration_comp.py` — collapse verbose docstrings on all 4 functions to single-line. Trim module docstring.
- **Acceptance criteria:**
  - All functions have single-line docstrings (no Args/Returns blocks)
  - Module docstring is 1-2 lines
  - No logic changes
- **Verification:** `ruff check nomarr/components/library/tag_hydration_comp.py && python -m mypy nomarr/components/library/tag_hydration_comp.py --no-error-summary`

## S2: Playlist import
- **Depends on:** none
- **Scope:** `nomarr/components/playlist_import/{deezer_fetcher_comp,spotify_fetcher_comp,url_parser_comp,metadata_normalizer_comp}.py` — collapse verbose docstrings on all functions. Remove redundant inline comments in url_parser_comp.py.
- **Acceptance criteria:**
  - All functions have single-line docstrings
  - No Args/Returns/Examples blocks
  - No redundant "# Try Spotify web URL" inline comments
- **Verification:** `ruff check nomarr/components/playlist_import/ && python -m mypy nomarr/components/playlist_import/ --no-error-summary`

## S3: Metadata + analytics overview
- **Depends on:** none
- **Scope:**
  - `nomarr/components/metadata/entity_seeding_comp.py` — collapse verbose docstrings on _derive_artists, _extract_entity_tags, _build_song_tag_entries, _build_entity_tag_map, _build_scan_tag_map. Remove section separator comments (# ====). Remove em-dash prefixed comments (# — artist —).
  - `nomarr/components/analytics/collection_overview_comp.py` — trim redundant docstring.
- **Acceptance criteria:**
  - All functions have single-line docstrings
  - No section separators or em-dash comments
  - No logic changes
- **Verification:** `ruff check nomarr/components/metadata/entity_seeding_comp.py nomarr/components/analytics/collection_overview_comp.py && python -m mypy nomarr/components/metadata/entity_seeding_comp.py nomarr/components/analytics/collection_overview_comp.py --no-error-summary`

## S4: Library — reconcile_paths_comp.py
- **Depends on:** none
- **Scope:** `nomarr/components/library/reconcile_paths_comp.py` — collapse verbose docstrings on reconcile_library_paths (usage example + Examples:) and _handle_invalid_path. Trim module docstring.
- **Acceptance criteria:**
  - All functions have single-line docstrings
  - No Examples: blocks
  - No logic changes
- **Verification:** `ruff check nomarr/components/library/reconcile_paths_comp.py && python -m mypy nomarr/components/library/reconcile_paths_comp.py --no-error-summary`

## S5: Workers + file batch scanner
- **Depends on:** none
- **Scope:**
  - `nomarr/components/workers/worker_discovery_comp.py` — collapse verbose docstrings on all 8+ functions
  - `nomarr/components/library/file_batch_scanner_comp.py` — collapse verbose docstrings on scan_folder_files, _compute_normalized_path
- **Acceptance criteria:**
  - All functions have single-line docstrings (no Args/Returns blocks)
  - No logic changes
- **Verification:** `ruff check nomarr/components/workers/worker_discovery_comp.py nomarr/components/library/file_batch_scanner_comp.py && python -m mypy nomarr/components/workers/worker_discovery_comp.py nomarr/components/library/file_batch_scanner_comp.py --no-error-summary`

## S6: Platform
- **Depends on:** none
- **Scope:**
  - `nomarr/components/platform/resource_monitor_comp.py` — collapse verbose docstrings on all functions
  - `nomarr/components/platform/gpu_probe_comp.py` — collapse verbose docstring on probe_gpu_availability. Trim module docstring.
- **Acceptance criteria:**
  - All functions have single-line docstrings
  - Module docstrings trimmed to 1-2 lines
  - No logic changes
- **Verification:** `ruff check nomarr/components/platform/ && python -m mypy nomarr/components/platform/ --no-error-summary`

## S7: Tagging — remove _library_ns anti-pattern
- **Depends on:** none
- **Scope:** `nomarr/components/tagging/tag_stats_comp.py` — remove `_library_ns()` wrapper (returns `cast("Any", db.library)`), replace all callers with direct `db.library` access. Remove unused `cast` import.
- **Acceptance criteria:**
  - `_library_ns` function removed
  - All callers use `db.library` directly
  - Zero remaining references to `_library_ns` in codebase
  - `ruff check` + `mypy` pass
- **Verification:** `ruff check nomarr/components/tagging/tag_stats_comp.py && python -m mypy nomarr/components/tagging/tag_stats_comp.py --no-error-summary`

## S8: Processing — file_write_comp.py
- **Depends on:** none
- **Scope:** `nomarr/components/processing/file_write_comp.py` — collapse verbose docstrings on save_mood_tags, save_mood_tags_batch, get_file_for_writing, get_nomarr_tags, release_file_claim. Trim _MOOD_TIER_NAMES comment.
- **Acceptance criteria:**
  - All functions have single-line docstrings
  - _MOOD_TIER_NAMES comment is one line
  - No logic changes
- **Verification:** `ruff check nomarr/components/processing/file_write_comp.py && python -m mypy nomarr/components/processing/file_write_comp.py --no-error-summary`

## S9: Analytics — analytics_comp.py
- **Depends on:** none
- **Scope:** `nomarr/components/analytics/analytics_comp.py` — collapse verbose docstrings on compute_tag_frequencies, compute_tag_correlation_matrix, compute_mood_distribution, compute_artist_tag_profile, compute_tag_co_occurrence, compute_dominant_vibes.
- **Acceptance criteria:**
  - All 6 functions have single-line docstrings (no Args/Returns blocks)
  - No logic changes
- **Verification:** `ruff check nomarr/components/analytics/analytics_comp.py && python -m mypy nomarr/components/analytics/analytics_comp.py --no-error-summary`

# Dependency DAG
```
Layer 0 (all parallel, all file-disjoint):
  S1 ── S2 ── S3 ── S4 ── S5 ── S6 ── S7 ── S8 ── S9
```
All 9 sub-tasks touch completely disjoint files. No dependencies between them.

# Aggregate Verification
- `ruff check nomarr/components/ --output-format concise`
- `python -m mypy nomarr/components/ --no-error-summary`
- `python -m pytest tests/ -x -q --no-header`

# Done Signal
RW_DONE_X9K2M7V4
