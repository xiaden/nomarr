# Goal
perform code maintenance passes over the backend services, moving to have a better composition, DRY patterns, typing closer to the examples in V2, and clear, Meaningful logging/error tracing @nomarr/components/ . additionally, remove all AI generated slop introduced in backend code files.

This includes:
- Extra comments that a human wouldn't add or is inconsistent with the rest of the file
- Extra defensive checks or try/catch blocks that are abnormal for that area of the codebase (especially if called by trusted / validated codepaths)
- Casts to any to get around type issues
- Any other style that is inconsistent with the file
- Unnecessary emoji usage

# Budget
max_rounds: 5

# Baseline
- ruff: clean (0 issues on nomarr/components/)
- mypy: clean (0 errors, 120 source files)
- pytest: no tests in nomarr/ directory (0 collected)

# Sub-tasks

## S1: Fix cast("Any") violations and narrow except blocks in ml_vector_maintenance_comp.py
- Depends on: none
- Scope:
  - `nomarr/components/ml/vectors/ml_vector_maintenance_comp.py` — 3 cast("Any") calls (lines 23, 129, 149), 1 cast("int") call (line 24), 1 cast("list[dict[...]]") call (line 34), 2 broad except Exception blocks (lines 96, 130)
- Pattern: single-file focused fix
- Acceptance criteria:
  - `cast("Any", ...)` violations removed: `_load_vector_docs` parameter changed from `object` to `VectorCollection` (imported from `nomarr.persistence.schema_types`); all 3 `cast("Any", ...)` calls removed
  - Redundant `cast("int", ...)` on `ops.count()` removed (count() returns int on VectorCollection)
  - Redundant `cast("list[dict[str, Any]]", ...)` on `ops.get.in_(...)` removed (return type already list[dict[str, Any]])
  - `cast("Any", cold_ops).update_many(...)` at line 149 replaced with direct call (type is now known)
  - The one remaining cast at call site: `get_cold_namespace()` returns `VectorsTrackColdNamespace` protocol, so a `cast(VectorCollection, cold_ops)` at the call site in `backfill_genres` is acceptable (narrowing a protocol to its runtime type, not casting to Any)
  - `except Exception` at line 96 (derive_embed_dim) narrowed to `(OSError, RuntimeError)` — the try block only does filesystem I/O and onnxruntime inference
  - `except Exception` at line 130 (backfill_genres) narrowed to `(KeyError, ValueError, RuntimeError)` — the try block does vector doc loading and dict access
- Verification: `python -m ruff check nomarr/components/ml/vectors/ml_vector_maintenance_comp.py && python -m mypy nomarr/components/ml/vectors/ml_vector_maintenance_comp.py`

## S2: Fix cast proliferation on mutagen types in tagging_writer_comp.py
- Depends on: none
- Scope:
  - `nomarr/components/tagging/tagging_writer_comp.py` — 5 string-literal casts (lines 61, 135, 140, 199, 205) used to bypass mutagen's loose typing
- Pattern: single-file focused fix
- Acceptance criteria:
  - `cast("str", getattr(frame, "desc", ""))` at line 61 replaced with: assign to variable, use `isinstance(v, str)` guard before calling `.startswith()`, or use `str(getattr(frame, "desc", ""))` for safe coercion
  - `cast("dict[str, list[MP4FreeForm]]", mp4.tags)` at line 135 replaced with: after `mp4.tags is None` check, verify `isinstance(mp4.tags, dict)` to narrow, then use the dictionary directly
  - `cast("list[str]", tag_value)` at line 140 removed — the `isinstance(tag_value, list) and all(isinstance(x, str) for x in tag_value)` on line 139 already narrows this; assign to a typed variable if needed
  - `cast("dict[str, list[str]]", vorbis_file.tags)` at line 199 replaced with: after `vorbis_file.tags is None` check, verify `isinstance(vorbis_file.tags, dict)` to narrow
  - `cast("list[str]", tag_value)` at line 205 removed — same reasoning as line 140; the isinstance check above already narrows
  - No `cast()` calls remain in the file (verify with search)
  - Import of `cast` from typing removed if no other casts remain
- Verification: `python -m ruff check nomarr/components/tagging/tagging_writer_comp.py && python -m mypy nomarr/components/tagging/tagging_writer_comp.py`

## S3: Fix cast("dict[str, Any] | None") patterns and narrow except blocks in ml_capacity_probe_comp.py
- Depends on: none
- Scope:
  - `nomarr/components/ml/resources/ml_capacity_probe_comp.py` — 2 cast("dict[str, Any] | None", ...) calls (lines 99, 127), 1 import of `cast` (line 20), 2 broad except Exception blocks (lines 175, 323)
- Pattern: single-file focused fix
- Acceptance criteria:
  - `cast("dict[str, Any] | None", db.app.get_lock(...))` at line 99 removed — `get_lock()` returns `dict | None` natively (see `nomarr/persistence/api/application.py:234`); return type annotation changed from `dict[str, Any] | None` to `dict | None` to match source; caller already uses `isinstance(existing, dict)` for narrowing
  - `cast("dict[str, Any] | None", db.app.get_config_option(...))` at line 127 removed — `get_config_option()` returns `dict | None` natively (see `nomarr/persistence/api/application.py:316`); return type annotation changed from `dict[str, Any] | None` to `dict | None`; callers already check `if estimate is not None` before key access
  - Import of `cast` from typing removed
  - `except Exception` at line 175 (compute_model_set_hash) narrowed to `OSError` — the try block only does filesystem walking
  - `except Exception` at line 323 (_run_capacity_probe) narrowed to `(OSError, RuntimeError, ValueError)` — the try block does filesystem/process I/O and model inference
- Verification: `python -m ruff check nomarr/components/ml/resources/ml_capacity_probe_comp.py && python -m mypy nomarr/components/ml/resources/ml_capacity_probe_comp.py`

## S4: Fix broad except Exception blocks without justification across remaining components
- Depends on: none
- Scope:
  - `nomarr/components/ml/vectors/ml_vector_persist_comp.py` — line 142: narrow to `(OSError, RuntimeError)` (filesystem + model operations)
  - `nomarr/components/platform/gpu_monitor_comp.py` — lines 59, 68, 85, 92: narrow to `(subprocess.CalledProcessError, OSError, ValueError)` (subprocess + filesystem)
  - `nomarr/components/platform/gpu_probe_comp.py` — line 136: narrow to `(OSError, RuntimeError)` (subprocess/filesystem)
  - `nomarr/components/platform/arango_bootstrap_comp.py` — line 68: narrow to `(RuntimeError, ValueError)` (ArangoDB operations)
  - `nomarr/components/library/reconcile_paths_comp.py` — lines 102, 158: narrow to `(OSError, ValueError)` (filesystem operations)
  - `nomarr/components/processing/file_write_comp.py` — line 134: narrow to `(OSError, RuntimeError)` (filesystem I/O)
  - `nomarr/components/metadata/entity_seeding_comp.py` — line 202: narrow to `(RuntimeError, ValueError, KeyError)` (DB operations + dict access)
  - `nomarr/components/library/move_detection_comp.py` — lines 195, 264, 297: narrow to `(OSError, ValueError)` (filesystem operations)
  - `nomarr/components/library/file_batch_scanner_comp.py` — line 144: narrow to `(OSError, RuntimeError)` (filesystem + DB operations)
  - `nomarr/components/platform/resource_monitor_comp.py` — lines 125, 268: narrow to `(OSError, RuntimeError)` (subprocess/filesystem)
- Files with pre-existing justification comments (arango_first_run_comp.py:82, library_scan_file_ops_comp.py:207) are EXCLUDED — they are already justified
- Pattern: fan-out across many files, each independent
- Acceptance criteria:
  - Every `except Exception` in the scoped files replaced with a more specific exception tuple appropriate to the operations in the try block
  - No regression on ruff or mypy
- Verification: `python -m ruff check nomarr/components/ml/vectors/ml_vector_persist_comp.py nomarr/components/platform/gpu_monitor_comp.py nomarr/components/platform/gpu_probe_comp.py nomarr/components/platform/arango_bootstrap_comp.py nomarr/components/library/reconcile_paths_comp.py nomarr/components/processing/file_write_comp.py nomarr/components/metadata/entity_seeding_comp.py nomarr/components/library/move_detection_comp.py nomarr/components/library/file_batch_scanner_comp.py nomarr/components/platform/resource_monitor_comp.py && python -m mypy nomarr/components/ml/vectors/ml_vector_persist_comp.py nomarr/components/platform/gpu_monitor_comp.py nomarr/components/platform/gpu_probe_comp.py nomarr/components/platform/arango_bootstrap_comp.py nomarr/components/library/reconcile_paths_comp.py nomarr/components/processing/file_write_comp.py nomarr/components/metadata/entity_seeding_comp.py nomarr/components/library/move_detection_comp.py nomarr/components/library/file_batch_scanner_comp.py nomarr/components/platform/resource_monitor_comp.py`

# Dependency DAG
S1 ── (parallel) ──▶
S2 ── (parallel) ──▶
S3 ── (parallel) ──▶
S4 ── (parallel) ──▶

All four sub-tasks are in Layer 0: fully independent scopes (no file overlap), all can execute concurrently.

# Aggregate Verification
- `python -m ruff check nomarr/components/`
- `python -m mypy nomarr/components/`
- `grep -rn 'cast("' nomarr/components/ml/vectors/ml_vector_maintenance_comp.py nomarr/components/tagging/tagging_writer_comp.py nomarr/components/ml/resources/ml_capacity_probe_comp.py` — should return empty (no remaining string-literal casts in target files)
- `grep -rn 'except Exception' nomarr/components/ml/vectors/ml_vector_maintenance_comp.py nomarr/components/ml/vectors/ml_vector_persist_comp.py nomarr/components/ml/resources/ml_capacity_probe_comp.py nomarr/components/platform/gpu_monitor_comp.py nomarr/components/platform/gpu_probe_comp.py nomarr/components/platform/arango_bootstrap_comp.py nomarr/components/library/reconcile_paths_comp.py nomarr/components/processing/file_write_comp.py nomarr/components/metadata/entity_seeding_comp.py nomarr/components/library/move_detection_comp.py nomarr/components/library/file_batch_scanner_comp.py nomarr/components/platform/resource_monitor_comp.py` — should only show the two justified uses (arango_first_run_comp.py:82, library_scan_file_ops_comp.py:207)

# Done Signal
<promise>RW_DONE_c0424f91</promise>
