# Goal
perform code maintenance passes over the backend services, moving to have a better composition, DRY patterns, typing closer to the examples in V2, and clear, Meaningful logging/error tracing @nomarr/components/  . additionally, remove all AI generated slop introduced in backend code files.

This includes:
- Extra comments that a human wouldn't add or is inconsistent with the rest of the file
- Extra defensive checks or try/catch blocks that are abnormal for that area of the codebase (especially if called by trusted / validated codepaths)
- Casts to any to get around type issues
- Any other style that is inconsistent with the file
- Unnecessary emoji usage

# Baseline
- ruff check nomarr/components/: clean (0 errors)
- mypy nomarr/components/: clean (0 errors)  
- pytest tests/: 1715 passed, 2 skipped
- Working tree: clean

# Sub-tasks

## S1: library/ — Cast reduction, redundant comment removal, style consistency
- Depends on: none
- Scope: all .py files in nomarr/components/library/
  - library_file_query_comp.py — heavy cast() usage (20+), dict[str, Any] patterns
  - library_file_state_comp.py — cast() usage (10+)
  - library_records_comp.py — cast() usage (10+), redundant comments
  - library_scan_file_ops_comp.py — cast() usage, redundant step-by-step comments
  - tag_hydration_comp.py — dict[str, Any] patterns
  - file_batch_scanner_comp.py — redundant "# Process each file" style comments
  - scan_lifecycle_comp.py — redundant comments
  - metadata_extraction_comp.py — except Exception with partial justification
  - folder_analysis_comp.py, library_admin_comp.py, library_file_mutation_comp.py, library_id_comp.py, library_root_comp.py, library_scan_state_comp.py, library_watch_config_comp.py, list_libraries_comp.py, missing_file_detection_comp.py, move_detection_comp.py, reconcile_paths_comp.py, reconciliation_comp.py, search_files_comp.py, update_library_metadata_comp.py, work_status_comp.py, file_sync_comp.py, file_tags_comp.py — style, redundant comments
  - songs/song_class.py — check for slop patterns
- Pattern: fan-out (parallel with other component sub-tasks)
- Acceptance criteria:
  - No self-describing comments left (e.g., "# Slice to top_n", "# Get audio files in this folder", "# Process each file" — comments that restate what the next line of code already says)
  - cast() reduced where safe: replace `cast("SomeType", db.x.y(...))` with intermediate variables that narrow the type via `isinstance` checks, or annotate callers with proper return types; keep cast() only where truly necessary (persistence return shapes that can't be narrowed in-line)
  - Replace bare `dict[str, Any]` with `TypedDict` subclasses for frequently-used document shapes (e.g., LibraryFileDoc, FolderDoc, ScanStateDoc) that are used by multiple functions in the same file — define them at file top
  - No functional behavior changes (all existing tests must pass)
  - Consistent logging: all log messages use structured `%s`-style formatting (no f-strings in logs), meaningful context in messages
  - Consistent import style within each file (stdlib → third-party → first-party groups)
- Verification: `ruff check nomarr/components/library/ && mypy nomarr/components/library/ && pytest tests/ -x --tb=short`

## S2: tagging/ — Cast reduction, deduplicate helpers, redundant comment removal
- Depends on: none
- Scope: all .py files in nomarr/components/tagging/
  - tag_query_comp.py — heavy cast() usage (15+ calls), some dict[str, Any] patterns
  - tag_stats_comp.py — heavy cast() usage (15+ calls), has duplicate `_numeric_value` that also exists in tag_query_comp.py
  - tag_write_comp.py — cast() usage
  - tagging_writer_comp.py — cast() usage
  - tagging_reader_comp.py — cast() usage
  - tagging_remove_comp.py — cast() usage, except Exception patterns
  - tag_parsing_comp.py — style check
  - safe_write_comp.py — style check, except Exception justification
  - mood_labels_comp.py, tag_cleanup_comp.py, tag_normalization_comp.py, tagging_aggregation_comp.py, tagging_reconstruction_comp.py — style/comment cleanup
- Pattern: fan-out
- Acceptance criteria:
  - Duplicate `_numeric_value` helper consolidated: one canonical version (likely in tag_query_comp.py), the other (tag_stats_comp.py) imports and reuses it
  - cast() reduced: replace with isinstance narrowing where possible
  - No self-describing comments (comments that restate code)
  - Replace bare `dict[str, Any]` with TypedDict where a document shape is used by 2+ functions in the same file
  - No functional behavior changes
  - Consistent logging format within each file
- Verification: `ruff check nomarr/components/tagging/ && mypy nomarr/components/tagging/ && pytest tests/ -x --tb=short`

## S3: ml/ — Cast reduction, redundant comment removal, logging consistency, except Exception justification
- Depends on: none
- Scope: all .py files in nomarr/components/ml/ (audio/, calibration/, inference/, onnx/, resources/, vectors/)
  - audio/ml_audio_comp.py — except Exception, redundant comments
  - audio/ml_chromaprint_comp.py — except Exception with bare fallback
  - audio/ml_preprocess_comp.py — style
  - inference/ml_backbone_embed_comp.py — except Exception
  - inference/ml_head_pipeline_comp.py — except Exception
  - inference/ml_heads_comp.py — except Exception
  - inference/ml_embed_comp.py — style
  - inference/ml_output_stream_store_comp.py — style
  - calibration/ml_calibration_comp.py, ml_calibration_state_comp.py — style
  - onnx/ml_base.py — except Exception
  - onnx/ml_backbone.py, ml_head.py, ml_cache.py, ml_constants.py, ml_discovery_comp.py, ml_known_models_comp.py, ml_model_registry_comp.py, ml_session_comp.py — style/comment cleanup
  - resources/ml_capacity_probe_comp.py — except Exception
  - resources/ml_vram_probe_comp.py — except Exception (justified with comments)
  - resources/ml_tier_selection_comp.py, ml_timing_comp.py, ml_vram_coordinator_comp.py, ml_vram_oom_helper_comp.py, ml_worker_context_comp.py — style
  - vectors/ml_vector_maintenance_comp.py — cast("Any"), except Exception
  - vectors/ml_vector_persist_comp.py — except Exception without variable binding
  - vectors/ml_vector_idle_promotion_comp.py, ml_vector_pool_comp.py, ml_vector_registry_comp.py, ml_vector_retrieve_comp.py — cast/comment cleanup
- Pattern: fan-out
- Acceptance criteria:
  - No self-describing comments
  - cast() reduced: remove unnecessary casts, replace with isinstance narrowing
  - `except Exception` cases reviewed: keep only those with genuine justification (commented); add specific exception types to bare catches where the failure mode is predictable
  - Bare `except Exception` (without variable) in ml_vector_persist_comp.py — add `as e` and log the exception
  - Consistent structured logging throughout
  - No functional behavior changes
- Verification: `ruff check nomarr/components/ml/ && mypy nomarr/components/ml/ && pytest tests/ -x --tb=short`

## S4: analytics/ + infrastructure/ + metadata/ + processing/ — Cleanup
- Depends on: none
- Scope:
  - nomarr/components/analytics/analytics_comp.py — dict[str, Any] in function signatures, redundant debug logs
  - nomarr/components/analytics/collection_overview_comp.py — style
  - nomarr/components/analytics/mood_analysis_comp.py — dict[str, Any] patterns
  - nomarr/components/infrastructure/health_comp.py — style
  - nomarr/components/infrastructure/path_comp.py — redundant step-by-step comments throughout
  - nomarr/components/metadata/entity_seeding_comp.py — dict[str, Any] throughout, except Exception
  - nomarr/components/processing/file_write_comp.py — except Exception, style
- Pattern: fan-out
- Acceptance criteria:
  - No self-describing comments (especially in path_comp.py which has heavy step-by-step commenting)
  - Replace dict[str, Any] with TypedDict where a single shape is used by 2+ functions
  - cast() reduction where applicable
  - No functional changes
- Verification: `ruff check nomarr/components/analytics/ nomarr/components/infrastructure/ nomarr/components/metadata/ nomarr/components/processing/ && mypy nomarr/components/analytics/ nomarr/components/infrastructure/ nomarr/components/metadata/ nomarr/components/processing/ && pytest tests/ -x --tb=short`

## S5: navidrome/ + playlist_import/ + platform/ + workers/ — Cleanup
- Depends on: none
- Scope:
  - nomarr/components/navidrome/ — all .py files (taste_profile_comp.py has heavy redundant comments, descriptor_match_comp.py has cast() usage, navidrome_graph_comp.py has cast("Any"), templates_comp.py has dict[str, Any] patterns)
  - nomarr/components/playlist_import/ — all .py files (track_matcher_comp.py style, deezer_fetcher_comp.py, spotify_fetcher_comp.py, metadata_normalizer_comp.py, url_parser_comp.py)
  - nomarr/components/platform/ — all .py files (arango_bootstrap_comp.py, arango_first_run_comp.py, gpu_monitor_comp.py, gpu_probe_comp.py, locks_comp.py, migration_runner_comp.py, resource_monitor_comp.py)
  - nomarr/components/workers/ — worker_crash_comp.py, worker_discovery_comp.py
- Pattern: fan-out
- Acceptance criteria:
  - No self-describing comments (major focus: taste_profile_comp.py — "# Slice to top_n", "# Filter to tracks", "# Batch-fetch", "# Build file_id→vector mapping", "# Pair plays with their vectors", "# Group paired plays by genre", "# Compute per-cluster centroids", "# Handle untagged tracks", "# Cap clusters", "# If no clusters formed")
  - cast() reduced where possible
  - except Exception patterns reviewed — add justification comments for genuinely broad catches, add specific exception types where predictable
  - dict[str, Any] → TypedDict for reusable shapes
  - No functional changes
- Verification: `ruff check nomarr/components/navidrome/ nomarr/components/playlist_import/ nomarr/components/platform/ nomarr/components/workers/ && mypy nomarr/components/navidrome/ nomarr/components/playlist_import/ nomarr/components/platform/ nomarr/components/workers/ && pytest tests/ -x --tb=short`

# Dependency DAG
All sub-tasks are independent (S1-S5 touch mutually exclusive directories):
```
S1 ──▶ (none)
S2 ──▶ (none)
S3 ──▶ (none)
S4 ──▶ (none)
S5 ──▶ (none)
Layer 0: S1, S2, S3, S4, S5 (all parallel)
```

# Aggregate Verification
- `ruff check nomarr/components/`
- `mypy nomarr/components/`
- `pytest tests/ -x --tb=short`

# Done Signal
RW_DONE_k9m2x7p4
