# Production dependencies — minimum versions align with dockerfile.base
numpy==1.26.4              # Pinned: onnxruntime compatibility
scipy>=1.13.1
# Use onnxruntime-gpu to match dockerfile.base; falls back to CPU automatically
# if CUDA is unavailable (Windows dev without CUDA, or CI). Do NOT swap for the
# CPU-only 'onnxruntime' package — that silently drops CUDAExecutionProvider.
onnxruntime-gpu>=1.18.0
mutagen>=1.47.0
pyyaml>=6.0.2
fastapi>=0.115.0
uvicorn>=0.30.6
rich>=13.7.0
requests>=2.32.0
bcrypt>=4.2.0
python-arango>=8.0.0
watchdog>=6.0.0
psutil>=7.0.0
setproctitle>=1.3.0
rapidfuzz>=3.0.0
spotipy>=2.24.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0

# Development/QC tools
ruff>=0.14.0
mypy>=1.19.0
bandit>=1.7.5
vulture>=2.10
pydub>=0.25.1

# Complexity & Architecture Analysis
radon>=5.1.0
import-linter>=2.0

# Type stubs
types-PyYAML
types-requests


# Documentation site
mkdocs-material>=9.5.0