# Local override for music library mount
# 
# By default, compose.yaml expects X:/media to be accessible
# If you're on WSL2 and X: doesn't mount, use one of these options:
#
# OPTION 1: Mount via SMB inside container (recommended for WSL2)
# 1. Comment out the X:/media line in compose.yaml volumes section
# 2. After container starts: docker exec -it nomarr-app bash
# 3. apt-get update && apt-get install -y cifs-utils
# 4. mkdir -p /media
# 5. mount -t cifs //192.168.220.254/data/media /media -o username=guest,password=
#
# OPTION 2: Use Windows host path (if Docker Desktop file sharing enabled)
# MUSIC_LIBRARY_MOUNT=X:/media:/media:ro
#
# OPTION 3: Use different host directory
# MUSIC_LIBRARY_MOUNT=/some/local/path:/media:ro
