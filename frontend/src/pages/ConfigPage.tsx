/**
 * Configuration page route wrapper.
 *
 * The actual implementation is in features/config/ConfigPage.
 */

import { ConfigPage as ConfigFeature } from "../features/config/ConfigPage";

export function ConfigPage() {
  return <ConfigFeature />;
}
