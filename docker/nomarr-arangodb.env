# ArangoDB Environment Variables
# Copy to nomarr-arangodb.env and customize

# Root password - REQUIRED for first-run provisioning
# Used ONLY during initial onboarding to create database and app user
# Change this to a strong password in production!
ARANGO_ROOT_PASSWORD=change_this_root_password_in_production

# Disable anonymous access (always keep this 0)
ARANGO_NO_AUTH=0
